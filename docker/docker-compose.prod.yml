version: '3'
services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env
    environment:
      NODE_ENV: ${PROD_NODE_ENV}
      DEV_DATABASE_URL: ${PROD_DEV_DATABASE_URL}
